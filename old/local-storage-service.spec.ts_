import { LocalStorageService } from './local-storage-service.ts_';

describe('LocalStorageService', () => {
  it('should create an instance', () => {
    expect(new LocalStorageService()).toBeTruthy();
  });
});
